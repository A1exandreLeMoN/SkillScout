# SkillLens 路线图

本文档记录 SkillLens 的迭代方向、阶段目标和优先级。  
它的目标不是列愿望清单，而是把这套框架继续推进成一个**面向多种 Skill 形态的自动评测平台**。

---

## 1. 当前状态

SkillLens 目前已经具备这些能力：

- 基于 Skill Profile 自动发现目标
- 按 behavior_contract / capability / risk 自动生成测试用例
- 支持 `http_json` adapter
- 支持自然语言目标发现
- 执行健康与覆盖完整性分离判定
- 中文结构化报告输出

这意味着它已经不是“单一业务脚本”，而是一个可持续扩展的测试框架。

---

## 2. 核心原则

后续迭代都遵循这几条原则：

1. **覆盖率不等于通过**
   - 只要有未解释的执行层错误，报告就必须显式暴露。

2. **profile 驱动，而不是硬编码驱动**
   - 能从 profile 读的，不要写死在 runner 里。

3. **adapter 负责形态差异**
   - 不同 Skill 形态通过不同 adapter 适配。

4. **报告服务于决策**
   - 报告不是展示“看起来不错”，而是回答“能不能信、能不能上线、问题在哪”。

5. **优先扩展通用能力**
   - 不再围绕某个具体业务加规则。

---

## 3. 近期目标

### 3.1 扩展第二个 adapter

当前只有 `http_json`。下一步优先新增：

- `dialogue`
  - 适合纯对话型 Skill
  - 关注多轮上下文、意图保持、回答一致性

可选后续扩展：

- `tool`
  - 适合工具调用型 Skill
- `workflow`
  - 适合多步骤编排型 Skill
- `file`
  - 适合文件生成 / 修改型 Skill

### 3.2 补全 profile schema

继续细化 profile，让它能表达：

- 每个行为规范条目的输入约束
- 前置条件
- 预期断言模板
- 失败/边界场景
- 是否允许自动执行
- 是否必须人工确认

### 3.3 强化执行健康报告

继续把执行错误放到更显眼的位置：

- 网络错误
- 网关错误
- HTTP 错误
- 解析错误
- 插件或运行时异常

目标是让任何人一眼就能看出：
> 这是 skill 真失败，还是执行环境失败。

### 3.4 引入上下文澄清门

当 SkillLens 面对一个信息不够明确的新 Skill 时，不应该硬猜，而应该先补上下文，再继续测试。

这个机制的目标是：

- 让 LLM 在测试前先确认 skill 的类型与边界
- 避免把通用模板误套到不适合的 skill 上
- 让测试计划尽量覆盖主要场景，而不是只覆盖表面 action
- 在信息不足时，通过最少的问题补齐关键上下文

建议的澄清范围包括：

- 这个 skill 更像哪一类：查询型、对话型、工具型、工作流型、写操作型
- 用户最关心的测试重点是什么：接通、正确性、上下文、交互体验、风险控制
- 有没有必须严格遵守的行为约束：输出优先级、确认流程、副作用边界、异常降级

澄清原则：

1. **只问决策性问题**
   - 只问会直接影响测试策略的问题，不问无关细节。

2. **控制问题数量**
   - 默认最多 1~3 个问题，避免把自动化测试变成长对话。

3. **先补最小上下文**
   - 一旦足够生成合理测试计划，就停止追问。

4. **把用户回答转成结构化上下文**
   - 回答不能只停留在自然语言里，要落成可执行的测试输入。

5. **信息仍不足时要明确说明**
   - 如果补问后还是无法准确判断，就直接说明还缺什么，而不是继续盲测。

这个机制后续可以作为 SkillLens 的标准入口：

> 先识别信息缺口，再澄清，再生成测试计划，再执行测试。

---

## 4. 中期目标

### 4.1 建立多 Skill 形态样板库

为不同类型的 Skill 准备真实 profile 样板，至少覆盖：

- 问答类
- 搜索类
- 工具类
- 写操作类
- 工作流类
- 多轮对话类

目的不是做演示，而是验证：
> SkillLens 是否真的能适配多种 Skill 形态。

### 4.2 自动化 case 生成更智能

目前已经能按 profile 生成基础 case，后续希望做到：

- 按行为规范类型自动补正向 / 反向 / 边界 case
- 按 risk 自动补确认 / 拒绝 / 恢复 case
- 按上下文自动补多轮 case
- 按失败历史自动补回归 case

### 4.3 报告变成决策输出

后续报告应更明确输出：

- 是否可上线
- 是否需要补测
- 是否是执行环境问题
- 是否是 profile 配置问题
- 是否是 skill 本身问题

---

## 5. 长期目标

### 5.1 多 adapter 平台化

让 SkillLens 从“一个 runner”演化成：

> 一个可按 Skill 形态自动适配的测评平台

### 5.2 自然语言入口完全自动化

用户只需要说一句：

- “测试这个 skill”
- “测一下当前目录里的 skill”
- “测试刚刚那个 skill”

SkillLens 就能自动：

1. 找目标
2. 读 profile
3. 生成 case
4. 执行测试
5. 出报告

### 5.3 更强的结果解释能力

未来的报告不只是“Pass / Fail”，还要能回答：

- 为什么失败
- 哪个行为规范有缺口
- 哪些错误是环境噪音
- 哪些错误值得阻塞上线

---

## 6. 推荐优先级

如果按投入产出比排序，建议这样推进：

### 第一优先级
**新增第二个 adapter**

这是通用性的关键一步。

### 第二优先级
**补全 profile schema**

让自动生成 case 更准，判定更可靠。

### 第三优先级
**继续强化执行健康**

确保任何执行失败都无法被覆盖率掩盖。

### 第四优先级
**建立真实样板库**

让框架在真实 Skill 上接受检验。

---

## 7. 当前不建议做的事

- 不要回到固定数量用例思维
- 不要把业务规则继续写死在 runner 里
- 不要为了让报告好看而淡化执行错误
- 不要在 profile 不完整时强行“假装完全自动化”
- 不要把 profile 设计成用户必须手工填写的前置依赖

---

## 8. 版本口号

SkillLens 的下一阶段，不是“测一个 HTTP skill”，  
而是：

> **按 skill 形态自动适配、自动测试、自动判定。**

---

## 9. profile 的正确定位

profile 不是用户必须先提交的前置条件，而是 SkillLens 内部用于推导测试计划的中间产物。

正确的工作方式应该是：

1. **优先自动发现**
   - 从 `SKILL.md`、说明文档、示例返回、既有配置中自动识别可用信息。

2. **自动推导临时 profile**
   - 如果没有正式 profile，先生成一个本次测试可用的临时 profile。

3. **必要时再澄清补全**
   - 如果信息仍不足，再向用户询问少量关键问题。

4. **profile 可缓存、可复用、可留档**
   - 方便后续复测和审计，但不要求用户预先手工准备。

这条原则的目的很简单：

> **降低使用门槛，让 SkillLens 默认就能“看到新 skill、理解新 skill、开始测试新 skill”。**
